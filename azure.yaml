# Azure Developer CLI (azd) configuration
# Docs: https://learn.microsoft.com/azure/developer/azure-developer-cli/
name: smarthotel
metadata:
  template: smarthotel@0.0.1-beta
services:
  web:
    project: .
    host: appservice
    language: py
    docker:
      path: ./Dockerfile
      context: .
